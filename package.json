{
  "name": "part3",
  "version": "1.0.0",
  "description": "practice exercise",
  "license": "ISC",
  "author": "Nicolás Chiesa",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../part2-notes-frontend && npm run build && cp -r dist ../part3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
